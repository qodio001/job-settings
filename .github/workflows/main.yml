on:
  push:
    branches: [main]

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Setup process
        run: echo "Setup completed"

  build:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - name: Build process
        run: echo "Build completed"
